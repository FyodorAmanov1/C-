#include <iostream>

using namespace std;

int main()
{
    int a = 10, b = 25;
    
    if (a>b)
        cout << "It works a is more than b " << endl;
    else 
        cout << "It also works a is less than b " << endl;

while (++counter < 10){
    cout << counter<< endl;
    
}

unsigned int b = 5;
//b cannot be negative
for (int i = 0; i < 10, ++i)
//++i will run only after {} are executed, the last operation

do { //bla bla bla 

    
} while (i < 10) { 

    //bla bla bla
}

//this one will execute first and then check the while condition

    return 0;
}

//switch does work for the characters!
//for switch default doesn't have to have #break
// loops are just like if conditions but repeated many many times
//Do think of extreme cases
//prefix ++a is better than postfix b++, doesn't jeep cache --> faster
