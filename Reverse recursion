#include <iostream>
#include <string>
using namespace std;

void rev(string s){
    int static l = size(s);

    if (l != 0) {
        l--;
        cout << s[l];
        rev(s);
    }

}

int main() {
    string st = "ytisrevinU UN";
    rev(st);

    return 0;
}
